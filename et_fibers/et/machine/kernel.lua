-- Machine kernel: bottom machine vocabulary.
--
-- Keep this boring.  Semantic machinery lives in frontier/proofnet/world/commit.

local Base = require('et.kernel')

return {
  Status = Base.Status,
  Result = Base.Status,
  Util = Base.Util,
  Phase = Base.Phase,
}
