# Petri case study

This directory contains a trusted coloured Petri-net resource programme. It uses `fibers.internal.kernel` directly and is therefore a contributor case study rather than an example of ordinary public facility authoring.

The module is not installed and carries no version 1 compatibility promise. Its tests exercise witness enumeration, backtracking and transactional token movement.
